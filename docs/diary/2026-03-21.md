# Diary — 2026-03-21

## Summary
Fixed notification polling bugs and built a consolidated notification system with bell icon UI and single-endpoint backend.

## Q&A Log
**Q:** Fix notification system — badges only on reload, polling dependency bugs
**A:** Fixed `fetchUnreadCounts` missing from app init, removed stale dependencies from SpaceSelector polling and Timeline markNotificationsRead effects, added unread count refresh after addMemory/addSubstory.

**Q:** Consolidate all notifications (invites, join requests, activity) into a single polling call with a bell icon UI
**A:** Created `GET /notifications/summary` backend endpoint that returns unreadCounts, pendingInvites, joinRequests, and recent notifications in one call. Replaced separate `fetchMyInvites` + `fetchUnreadCounts` with single `fetchNotificationSummary` in store. Added bell icon with dropdown notification panel in SpaceSelector showing invites (with accept/decline), join requests (clickable to members modal), and activity notifications (clickable to navigate to space).

## Files Changed
| File | Change |
|------|--------|
| `MemoryWall-Claude backend/src/routes/notifications.ts` | Added `GET /summary` consolidated endpoint |
| `MemoryWall-Claude/src/api.ts` | Added `getNotificationSummary` API method |
| `MemoryWall-Claude/src/types/index.ts` | Added `NotificationSummary` interface |
| `MemoryWall-Claude/src/store/useStore.ts` | Added `fetchNotificationSummary`, replaced separate calls in init/login/addMemory/addSubstory |
| `MemoryWall-Claude/src/components/SpaceSelector.tsx` | Added Bell icon with notification dropdown panel, switched polling to use summary endpoint |
| `MemoryWall-Claude/src/components/Timeline.tsx` | Fixed markNotificationsRead dependency array |

**Q:** Implement feedback form — email-based + save in DB
**A:** Added full feedback feature: backend route (`POST /api/feedback`) with zod validation, Resend email to jagadeesh.sura22@gmail.com, and Prisma DB storage. Frontend: `FeedbackForm` modal component with type selector (Suggestion/Bug/Complaint), message textarea, success state. Wired into profile menu in SpaceSelector as "Send feedback" option.

| `MemoryWall-Claude backend/src/routes/feedback.ts` | New — POST /api/feedback route with zod, Resend email, Prisma save |
| `MemoryWall-Claude backend/src/index.ts` | Registered feedback route |
| `MemoryWall-Claude backend/prisma/schema.prisma` | Added Feedback model |
| `MemoryWall-Claude/src/api.ts` | Added `submitFeedback` method |
| `MemoryWall-Claude/src/components/FeedbackForm.tsx` | New — feedback modal component |
| `MemoryWall-Claude/src/components/SpaceSelector.tsx` | Added feedback modal type, menu item, and FeedbackForm render |
