# Diary — 2026-03-18

## Summary
Continued session: completed git stash/pull/push workflow for all mobile-friendly changes from previous session.

## Q&A Log
**Q:** Stash changes, get latest from git, merge, and push.
**A:** Stash was already created in prior session. Pulled latest (already up to date), popped stash with no conflicts, ran full pre-push checklist (tsc, vitest 37/37, build), committed and pushed as `7116e1c`.

**Q:** Implement external scaling services priority assessment — add error tracking, update SCALING-GUIDE.md with assessment.
**A:** Initially installed Sentry but reverted (only 14-day free trial). Instead built a zero-dependency in-memory error tracker: ring buffer of last 100 errors, queryable via `GET /api/admin/errors` (protected by `ADMIN_SECRET` header). Captures route errors, middleware errors, uncaught exceptions, and unhandled rejections with full stack traces, request IDs, and stats. Updated SCALING-GUIDE.md: marked error tracking complete, deferred 5 of 7 external services as premature optimization. All 96 backend tests pass.

## Files Changed
| File | Change |
|------|--------|
| `src/components/MomentEditor.tsx` | Full mobile touch support rewrite (drag, resize, pan, two-row toolbar, font picker bottom sheet) |
| `src/components/MemoryDetailC.tsx` | Cover photo expand, moment tile highlighting, three-dot menu on tap, crop originals, hide buttons during edit |
| `src/components/Timeline.tsx` | Compact header spacing, back button only, wider moment cards |
| `src/components/LoginPage.tsx` | Removed ParticleBackground |
| `src/components/SpaceSelector.tsx` | Removed ParticleBackground |
| `src/components/MemoryCard.tsx` | Updated "moments inside" text styling |
| `src/components/ImageCropper.tsx` | Crop enhancements |
| `src/components/CreateMemoryModal.tsx` | Google Photos removal (from prior session) |
| `src/components/MemoryDetail.tsx` | Google Photos removal (from prior session) |
| `src/components/MemoryDetailA.tsx` | Google Photos removal (from prior session) |
| `src/components/MemoryDetailB.tsx` | Google Photos removal (from prior session) |
| `src/types/index.ts` | Added `photoOriginals` to SubStory interface |
| `ios/App/App.xcodeproj/project.pbxproj` | Cleared DEVELOPMENT_TEAM to avoid team-sharing conflicts |
| `ios/App/App/Info.plist` | iOS config update |
| `backend/src/errorTracker.ts` | New — lightweight in-memory error ring buffer (100 entries) with stats |
| `backend/src/index.ts` | Added error tracking to all error paths + admin errors endpoint |
| `docs/SCALING-GUIDE.md` | Updated with priority assessment, marked error tracking complete, deferred 5 tasks |
