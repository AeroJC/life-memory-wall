# Diary — 2026-03-22

## Summary
Codebase cleanup — removed stale mock data, dead middleware, unused routes, deprecated components, and dead store/API/type code across both repos.

## Q&A Log
**Q:** Implement codebase cleanup plan to remove stale and dead code across backend and frontend.
**A:** Executed all 20 cleanup items: deleted 5 files (backend `data.ts`, `middleware/response.ts`; frontend `MemoryDetailA/B.tsx`, `NotificationPreviews.tsx`, `MediaPreviews.tsx`), removed dead functions (`formatSpaceWithMemories`, `blurPlaceholderUrl`, `fetchNotifications`, `fetchUnreadCounts`, `getUsers`, `getNotifications`, `getUnreadCounts`), removed unused selector hooks, deleted `Comment` interface and `comments` field, removed preview route blocks from App.tsx, updated backend types to match actual DB usage (added missing SubStory types, MemorySpace/User fields), cleaned up the cloudinary test, and fixed a cascading import of `formatSpaceWithMemories` in `spaces.ts`. All verification passed: tsc --noEmit (0 errors), tests (96+53 passed), build succeeded.

## Files Changed
| File | Change |
|------|--------|
| `backend/src/data.ts` | Deleted — mock data file |
| `backend/src/middleware/response.ts` | Deleted — unused response helpers |
| `backend/src/db.ts` | Removed `formatSpaceWithMemories()` |
| `backend/src/index.ts` | Removed `responseHelpers` import and mount |
| `backend/src/routes/notifications.ts` | Removed `GET /` and `GET /unread-count` routes |
| `backend/src/routes/spaces.ts` | Removed `formatSpaceWithMemories` from import |
| `backend/src/types.ts` | Updated to match actual usage (added missing SubStory types, MemorySpace/User fields) |
| `frontend/src/components/MemoryDetailA.tsx` | Deleted — old design variant |
| `frontend/src/components/MemoryDetailB.tsx` | Deleted — old design variant |
| `frontend/src/components/NotificationPreviews.tsx` | Deleted — preview component |
| `frontend/src/components/MediaPreviews.tsx` | Deleted — preview component |
| `frontend/src/App.tsx` | Removed preview lazy imports and hash route blocks |
| `frontend/src/store/useStore.ts` | Removed `fetchNotifications`, `fetchUnreadCounts`, 10 unused selector hooks |
| `frontend/src/api.ts` | Removed `getUsers`, `getNotifications`, `getUnreadCounts`, cleaned unused import |
| `frontend/src/cloudinary.ts` | Removed `blurPlaceholderUrl()` |
| `frontend/src/types/index.ts` | Removed `Comment` interface and `comments` field from Memory |
| `frontend/src/__tests__/cloudinary.test.ts` | Removed `blurPlaceholderUrl` test and import |
